kind = "web"
previewPath = "/"
title = "DrépaConnect"
version = "1.0.0"
id = "artifacts/drepaconnect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23586

[services.development]
run = "pnpm --filter @workspace/drepaconnect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/drepaconnect", "run", "build" ]
publicDir = "artifacts/drepaconnect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23586"
BASE_PATH = "/"
